!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Sellout'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Sellout'}"}
    = stylesheet_link_tag "application", media: "all"
    %script{src:"http://maps.googleapis.com/maps/api/js?sensor=false" }
    %script{src:"http://maps.googleapis.com/maps/api/js?libraries=places&sensor=false"}
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body(ng-app="selloutApp")
    / Start Wrapper
    #wrapper
      = render "layouts/navigation"
      .container
        = render "layouts/messages"
        = yield